services:
  notifiarr:
    container_name: notifiarr
    hostname: notifiarr
    image: golift/notifiarr
    # restart: unless-stopped
    ports:
      - "5454:5454"
    networks:
      - proxy
    volumes:
      - ${HOME}/pods/notifiarr/config:/config
      - /var/run/utmp:/var/run/utmp
      - /etc/machine-id:/etc/machine-id
